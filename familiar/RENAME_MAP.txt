Renaming map for familiar crate (confirmed scope: code, configs, env vars, migrations)

Core mappings (old -> new):
- agent (concept/variable) -> familiar
- tools (module/dir) -> grimoire
- Tool (struct) -> Spell
- ToolCall/tool_calls -> SpellCast / spell_casts (DB column)
- tool_call_id -> spell_cast_id (DB column)
- ToolResult -> SpellResult
- Plan/Workflow -> Ritual
- spawn -> summon (but keep tokio::spawn calls unchanged)
- Invocation/Incantation (prompt) -> Incantation (use in docs/UI)

File/module suggestions:
- src/tools/ -> src/spells/ or src/grimoire/
- file_tool.rs -> file_spell.rs
- script_tool.rs -> script_spell.rs
- outline_tool.rs -> outline_spell.rs

DB migrations:
- migrations/001_init.sql: rename column tool_calls -> spell_casts, tool_call_id -> spell_cast_id
- Provide SQL migration + rollback using ALTER TABLE RENAME COLUMN

Frontend/UI:
- client/src/store/auth.tsx: TOKEN_KEY = "familiar_token" -> optional rename to "familiar_token" (confirm exact key)
- client strings: "Familiar" -> keep or change to localized display name

Notes:
- External ds_api types (DeepseekAgent, Tool trait) kept unchanged.
- All replacements will be implemented with care: update module paths, use rust-analyzer/cargo build to resolve errors, and run tests.
